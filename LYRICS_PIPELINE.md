# 统一歌词识别与选取方案

本文档说明声迹服务端的统一歌词引擎，包括元数据提取、歌词候选召回、音频验证、时间轴校准、结果分级、缓存和后续扩展方向。

## 1. 设计目标

统一歌词引擎整合了两种处理思路：

- 以当前视频音频的 Whisper 识别结果作为验证依据。
- 优先复用 B站字幕和在线结构化歌词，减少纯语音识别错误。
- 支持浏览器插件的局部片段识别。
- 支持翻唱、现场、演唱会和版本时长差异。
- 所有联网歌词都必须经过当前视频音频验证，不能仅凭歌名直接采用。
- 在线歌词不可靠时保留 Whisper 原始结果，不让识别结果完全丢失。

核心原则：

> 字幕和在线歌词负责提供候选，Whisper 负责验证当前视频，最终由音频证据和时间轴质量决定是否采用。

## 2. 系统边界

### 浏览器扩展

`browser-extension/` 负责：

- 获取当前 B站页面 URL。
- 选择识别时间范围。
- 创建和轮询识别任务。
- 显示、编辑和同步歌词。
- 保存用户修改、时间微调和掌握状态。

扩展不负责在线歌词候选排序，也不需要用户选择歌曲语言。

### 服务端

服务端负责：

- 解析 B站视频和分P信息。
- 根据视频元数据自动判断识别语言。
- 下载并截取目标音频。
- 使用 Faster-Whisper 生成文本和时间锚点。
- 获取 B站字幕、网易云、LRCLIB 和可信网页歌词候选。
- 对所有候选执行元数据、音频和时间轴验证。
- 返回最佳歌词或 Whisper 回退结果。

主要实现文件：

- `transcribe_service.py`：任务识别流程、音频处理和最终结果组装。
- `server.py`：元数据提取、候选获取、歌词清洗、音频对齐和候选选择。
- `lyrics_config.json`：歌词搜索、候选过滤和时间轴阈值。
- `cache.py`：识别结果缓存和缓存版本。

## 3. 总体流程

```text
B站视频或分P
  ↓
解析视频元数据
  ↓
自动判断识别语言
  ↓
下载并截取目标音频
  ↓
Whisper 识别当前片段
  ↓
获取结构化歌词候选
  ├─ B站字幕
  ├─ 网易云歌词
  └─ LRCLIB
  ↓
统一候选竞争
  ├─ 元数据匹配
  ├─ 音频文本匹配
  ├─ 时间轴质量
  └─ 来源质量
  ↓
结构化来源无可靠结果时
  ↓
可信歌词网页搜索与抽取
  ↓
分级输出
  ├─ 同步歌词
  ├─ 重建时间轴
  ├─ 仅校正文案
  └─ Whisper 原始结果
```

## 4. 元数据归一化

服务端综合以下信息推断歌曲：

- 当前分P标题。
- 视频总标题。
- 视频描述。
- UP主名称。
- 标签和分类。
- B站返回的结构化字段。

标准化结果包括：

```json
{
  "song_title": "此生不换",
  "artist": "黄霄雲",
  "language": "zh",
  "version": "live_cover",
  "is_live": true,
  "is_cover": true,
  "is_instrumental": false,
  "version_terms": ["现场", "翻唱"]
}
```

### 标题处理原则

标题中的信息分为两类。

展示噪声可以从搜索标题中清理：

- 4K、8K、1080P、HD。
- MV、官方视频、动态歌词。
- 中文字幕、修复版、完整版。

版本信息不能完全丢弃，应保留为候选评分依据：

- Live、现场、演唱会。
- 翻唱、Cover。
- Remix、重制。
- 伴奏、Instrumental。
- 剪辑版、变速版。

### 自动语言判断

当插件发送 `language=auto` 时，服务端根据分P标题、总标题、描述、标签等内容判断：

- 中文：`zh`
- 日文：`ja`
- 韩文：`ko`
- 明显英文：`en`
- 无法可靠判断：交给 Whisper 自动检测

用户不需要手动选择语言。

## 5. 歌词来源

### 5.1 B站字幕

B站字幕是与当前视频绑定最紧密的来源。字幕优先级为：

1. 人工中文字幕。
2. 人工其他语言字幕。
3. 自动中文字幕。
4. 自动其他语言字幕。

字幕候选保留：

- 字幕语言。
- 字幕名称。
- 是否自动生成。
- 字幕正文和时间轴。
- 当前视频推断出的歌名和歌手。

B站字幕不会仅因存在就直接采用。它必须通过统一的来源一致性和音频验证，避免把解说字幕、节目对白或其他文本当成歌词。

### 5.2 网易云歌词

网易云提供结构化歌曲和 LRC 候选。处理流程：

1. 根据歌名和歌手搜索歌曲。
2. 获取候选歌曲名、歌手和时长。
3. 获取候选 LRC。
4. 生成统一候选对象。
5. 使用当前视频音频验证。

网易云不可访问或无结果时，不影响其他来源。

### 5.3 LRCLIB

LRCLIB 提供同步歌词和普通歌词。候选主要根据以下信息召回：

- 歌名和歌手。
- 仅歌名。
- 视频推断时长。

即使 LRCLIB 返回同步歌词，也不能直接采用，仍需验证当前音频是否与候选歌词一致。

### 5.4 可信歌词网页

当 B站字幕、网易云和 LRCLIB 都没有可靠结果时，系统再搜索普通网页歌词。

支持的歌词抽取方式包括：

- 页面中的 LRC 文本。
- HTML 正文歌词。
- JSON-LD 中的歌词字段。
- 搜索结果摘要中的歌词片段。

网页访问受允许域名、公共地址验证和 SSRF 防护约束。页面导航、广告、评论、版权说明和登录提示等内容会被过滤。

## 6. 统一候选格式

不同来源会转换成统一候选结构：

```json
{
  "provider": "lrclib",
  "track_name": "此生不换",
  "artist_name": "青鸟飞鱼",
  "duration": 267,
  "source_url": "https://...",
  "lrc_text": "[00:00.00]...",
  "plain_text": "...",
  "metadata_score": 0.94,
  "provider_quality": 0.90
}
```

统一结构使 B站字幕、网易云、LRCLIB 和网页歌词可以进入同一套验证与排序流程。

## 7. 候选验证与选择

候选不是按照来源返回顺序直接采用，而是综合以下证据竞争：

1. 音频锚点和歌词文本匹配。
2. 时间轴模型质量。
3. 歌名、歌手、时长和版本匹配。
4. 来源质量。

音频证据权重高于元数据。歌名和时长接近只能召回候选，不能证明歌词正确。

### 元数据匹配

检查内容包括：

- 歌名相似度。
- 歌手相似度。
- 候选时长与视频时长差异。
- 现场、翻唱、伴奏等版本关系。
- 来源页面标题与歌曲主题是否一致。

### 音频匹配

Whisper 结果用于寻找歌词锚点并计算：

- 可靠锚点数量。
- 平均文本相似度。
- 高置信锚点比例。
- 音频片段覆盖率。
- 锚点在时间上的分布。

只匹配到一个重复副歌或少量通用歌词时，不应将候选视为可靠。

### 来源质量

默认来源质量配置位于 `lyrics_config.json`：

```json
{
  "provider_quality": {
    "bilibili_subtitle": 0.72,
    "netease": 0.85,
    "lrclib": 0.9,
    "web": 0.55
  }
}
```

来源质量只用于候选排序，不会覆盖音频验证结果。

## 8. 翻唱和现场版本

当当前演唱者与歌词候选歌手不一致时，候选不会被立即拒绝，也不会被直接接受。

例如：

```text
当前演唱：黄霄雲
歌词来源：青鸟飞鱼
歌曲：此生不换
```

只有满足强音频条件时才允许使用原唱歌词：

- 歌名高度一致。
- 至少存在规定数量的可靠音频锚点。
- 平均音频置信度达到阈值。
- 高置信锚点比例达到阈值。
- 时间轴偏移或线性模型通过质量检查。

采用后会明确标记：

```json
{
  "match_type": "cover",
  "detected_artist": "黄霄雲",
  "source_artist": "青鸟飞鱼"
}
```

现场版还可能标记为 `live` 或 `version`，避免把原唱歌词误认为当前演唱者的正式歌词版本。

## 9. 时间轴校准

### 9.1 固定偏移模型

默认优先尝试固定偏移：

```text
视频时间 = 歌词时间 + offset
```

适合：

- 视频开头增加了前奏或口播。
- 歌词整体提前或延后。
- 短片段识别。
- 可用锚点较少。

系统会对候选偏移进行聚类，并检查偏移跨度是否稳定。

### 9.2 线性时间模型

长片段或整首歌曲可能存在速度差异，此时尝试：

```text
视频时间 = 歌词时间 × scale + offset
```

线性模型只有在以下条件全部满足时才会采用：

- 可靠锚点不少于 `linear_min_anchors`。
- 锚点时间跨度不少于 `linear_min_span`。
- `scale` 位于配置的安全范围内。
- 线性 RMSE 明显优于固定偏移模型。
- 平均文本相似度达到歌词同步阈值。

默认配置：

```json
{
  "linear_min_anchors": 4,
  "linear_min_span": 20.0,
  "linear_scale_min": 0.94,
  "linear_scale_max": 1.06,
  "linear_rmse_improvement": 0.15
}
```

短片段、锚点不足或拟合不稳定时继续使用固定偏移，不强制应用线性模型。

### 9.3 逐行重建

当候选只有普通歌词，或者原 LRC 时间轴不适合当前版本时，系统可以使用 Whisper 的 segment 和 word 时间重新建立歌词行时间轴。

适合：

- 现场版本漏句或重复副歌。
- 当前视频对原歌曲进行了剪辑。
- 网页只有纯文本歌词。
- 原始 LRC 时间轴质量较差。

## 10. 结果分级

当前服务保持以下兼容模式：

### `lrc_timeline_synced`

结构化 LRC 或字幕通过音频验证，并成功同步到当前视频。

### `lyrics_timeline_rebuilt`

歌词文本可靠，但原始时间轴不可直接使用，系统根据 Whisper 重新生成时间轴。

### `text_only`

歌词文本与音频具有一定一致性，但无法可靠恢复完整时间轴，仅校正文字。

### `none`

没有找到可靠联网歌词，保留 Whisper 原始识别结果。

未找到候选不代表识别任务失败。任务仍可成功返回模型识别文本。

## 11. Correction 返回结构

服务会在原有字段基础上返回扩展信息：

```json
{
  "mode": "lrc_timeline_synced",
  "provider": "lrclib",
  "confidence": 0.92,
  "match_type": "cover",
  "metadata": {
    "song_title": "此生不换",
    "detected_artist": "黄霄雲",
    "source_artist": "青鸟飞鱼",
    "language": "zh",
    "version": "live_cover"
  },
  "timeline": {
    "model": "linear",
    "offset": -8.94,
    "scale": 1.02,
    "rmse": 0.35,
    "anchor_count": 6
  },
  "attempted_providers": [
    "bilibili_subtitle",
    "netease",
    "lrclib"
  ]
}
```

字段说明：

- `provider`：最终使用的歌词来源。
- `confidence`：综合置信度。
- `match_type`：`exact`、`cover`、`live`、`version` 或 `fallback`。
- `metadata`：检测歌曲与歌词来源信息。
- `timeline`：时间轴模型及质量数据。
- `attempted_providers`：本次尝试过的来源。
- `reason`：未采用联网歌词时的原因。

## 12. 缓存设计

当前缓存保存：

- Whisper 原始识别结果。
- B站视频信息。
- 歌曲元数据。
- 最终歌词和 correction 信息。

缓存键应区分：

- 视频 URL 和分P。
- 识别开始与结束时间。
- 语言。
- Whisper 模型。
- 流水线和缓存版本。

歌词算法、语言判断或结果结构发生变化时，需要提升 `cache.py` 中的 `CACHE_VERSION`，使旧结果自动失效。

浏览器本地保存的用户修改和掌握状态不属于服务端算法缓存，不应因服务端升级而被覆盖。

## 13. 配置说明

主要配置位于 `lyrics_config.json`。

### `search`

控制：

- LRCLIB 和网易云是否启用。
- 每个来源最大候选数。
- 网页搜索查询模板和候选数量。

### `candidate_filter`

控制：

- 歌词页面噪声过滤。
- 来源标题和歌手匹配阈值。
- 翻唱候选所需的强音频条件。
- 文本校正最低要求。

### `lrc_sync`

控制：

- 歌词锚点相似度。
- 固定偏移聚类。
- 线性模型启用条件。
- 歌词裁剪和显示时间修正。

### `timeline_quality`

控制：

- 最小歌词行数。
- 最小锚定行数。
- 平均置信度。
- 高置信锚点比例。
- 音频覆盖率。

### `provider_quality`

设置不同来源的基础质量，但不能绕过音频验证。

## 14. 安全与可靠性

- 服务只接受允许的 B站页面 URL。
- 网页歌词请求必须通过公共地址验证和域名限制。
- 不允许网页歌词请求访问内网、环回地址和云元数据地址。
- 外部来源失败时继续尝试其他来源。
- 低质量候选不会覆盖 Whisper 结果。
- 歌手不同的歌词必须有强音频证据。
- 线性模型必须显著优于固定偏移才会启用。

## 15. 测试覆盖

当前测试覆盖：

- 复杂歌名和歌手提取。
- 中文、日文、韩文和英文语言判断。
- 现场、翻唱和伴奏版本识别。
- B站字幕解析、排序和主题一致性。
- 网易云歌词候选生成。
- LRCLIB 候选过滤。
- 多结构化来源竞争。
- 翻唱使用原唱歌词的强音频条件。
- 固定偏移时间轴。
- 长跨度线性时间轴。
- 短片段保持固定偏移。
- 高误差线性模型拒绝。
- correction 扩展字段兼容。
- 缓存版本失效。

测试命令：

```powershell
.\.venv\Scripts\python.exe -m unittest test_core.py
node browser-extension\test_background.js
```

## 16. 当前未实现内容

以下能力暂未纳入当前版本：

- 下载完整视频后执行画面歌词 OCR。
- 完整歌曲的多窗口采样 Whisper 模式。
- 完整字符级单调歌词路径搜索。
- 在插件 UI 中展示所有候选和评分详情。
- 允许用户手动切换歌词来源。

其中视频 OCR 成本较高，只适合作为所有字幕和在线歌词来源均失败后的最终回退，不应默认执行。

## 17. 后续演进建议

建议后续按以下顺序扩展：

1. 增加完整歌曲的前、中、后三段采样验证。
2. 增加更详细的候选诊断日志和服务监控指标。
3. 根据真实失败样本继续调整来源和时间轴阈值。
4. 最后再评估视频 OCR 回退，避免过早引入完整视频下载和重型 OCR 依赖。

任何扩展都应继续遵守以下规则：

> 新来源只负责增加候选，不能绕过当前视频音频验证直接覆盖结果。
